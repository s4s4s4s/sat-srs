---
name: sat-srs-map
description: Карта проекта sat-srs (C:\Users\sasha\dev\sat-srs) — PWA-замена Anki для подготовки к SAT. Читай ПЕРЕД тем, как искать файлы или обходить структуру: где планировщик FSRS, синхронизация с vault через GitHub, экраны и хранилище. Экономит обход дерева на каждой сессии.
---

# sat-srs — карта

React + TypeScript + Vite, PWA. Интервальные повторения для SAT. Карточки лежат не в приложении, а в Obsidian-vault и тянутся через GitHub API. Живёт на `s4s4s4s.github.io/sat-srs`, деплой — GitHub Actions (`.github/workflows/deploy.yml`).

## Ядро — `src/lib/`

| Файл | Размер | Ответственность |
|---|---:|---|
| `scheduler.ts` | 19 КБ | **FSRS: интервалы, состояния карточек. Сердце приложения.** |
| `store.ts` | 17 КБ | глобальное состояние |
| `report.ts` | 15 КБ | метрики и отчёты по прогрессу |
| `sync.ts` | 13 КБ | синхронизация с vault |
| `db.ts` | 8,5 КБ | локальное хранилище (IndexedDB) |
| `demo.ts` | 8,5 КБ | демо-режим |
| `journal.ts` | 8 КБ | журнал ответов |
| `yamlfm.ts` | 6 КБ | парсинг YAML-frontmatter карточек |
| `github.ts` | 6 КБ | GitHub API — чтение и запись карточек |
| `types.ts` | 6 КБ | доменные типы |
| `sound.ts` | 13 КБ | **звук интерфейса: партитуры голосов (чистые данные) + проигрыватель на Web Audio** |
| `coach.ts` | 12 КБ | **разбор «Почему?»: сборка запроса к модели (чистые функции) + поток ответа** |
| `daytime.ts`, `speech.ts` | 3 / 2 КБ | время суток; озвучка |

## Экраны — `src/screens/`

`Review.tsx` (61 КБ — самый крупный, основной цикл повторения), `Settings.tsx`, `Home.tsx`, `Stats.tsx`, `AddCard.tsx`, `Path.tsx`, `Summary.tsx`.

## Компоненты — `src/components/`

`FlameBuddy.tsx` (маскот стрика), `FjordScene.tsx` (фон), `Icon.tsx`, `Tex.tsx` (формулы).

Стили — один файл `src/styles.css` (66 КБ), не CSS-модули.

**Раскладок две, и обе живые.** По умолчанию телефонная лента; с 720 px колонка
расширяется, с 1024 px главный экран становится сеткой разделов, статистика —
двумя колонками потоком, а урок сжимается в компактный блок по центру окна
(классы экранов `s-home`, `s-review`, `s-stats`, `s-summary` в `screens/`).
Правку вёрстки проверять на обоих концах: `node scripts/shots.mjs <папка>
[dark|light] [phone|laptop|wide|<Ш>x<В>]` — по скриншотам одного размера регресс
другого не виден.

## Форматы повторения

Задача показа — пара «формат × опора»: `Format` (`intro | reveal | mc | type | prep`) и `Cue` (`sentence | meaning | word`) в `types.ts` и `scheduler.ts`. Выдаёт её `pickTask`; `pickFormat` оставлен как обёртка над ним.

Зрелые карточки (`State.Review`, без авторских `choices`/`answerNum`) идут по `REVIEW_CYCLE` — четыре шага по `reps`: предложение с пропуском, значение → слово, слово → значение, ввод по буквам. **Предложение ровно на одном шаге из четырёх** — иначе словарь проверяется узнаванием фразы, а не знанием слова. `degrade` снижает шаг, если карточке нечем его обеспечить (нет `meaning_ru`, мало дистракторов, выключен `typing`). Дистракторы: `mcDistractors` — английские слова, `meaningDistractors` — русские значения по лестнице «та же часть речи и уровень → та же часть речи → любые из раздела».

Знакомство (`intro`), математика и авторские варианты в ротацию не входят.

## Звук

`src/lib/sound.ts` разделён надвое намеренно. **Партитура** — чистая функция `score(voice, combo)`, возвращающая список нот `Note` (`{ freq, at, dur, gain, type }`) для каждого голоса из `VOICES`; она не знает про Web Audio и потому проверяется тестом в Node. **Проигрыватель** — `render` (кладёт ноты в любой `BaseAudioContext`) и `play` (озвучивает здесь и сейчас), плюс `primeAudio` для разблокировки звука по первому касанию.

Все голоса построены на одной пентатонике (`ROOT`, `PENT` в `sound.ts`): звуки накладываются друг на друга при быстрых ответах, и общая тональность не даёт им звучать грязно. Громкость сведена так, чтобы сумма одновременных нот не давала перегруза (`MASTER_GAIN`, проверка S5 в тесте).

Послушать, не запуская приложение: `npm run sounds` (`scripts/sound-wav.mjs`) — рендерит каждый голос в WAV через `OfflineAudioContext`.

Выключатель — в настройках (`sound` в `Settings`), доезжает до установленного PWA только через миграцию `SETTINGS_VERSION` в `store.ts`.

## Разбор «Почему?»

Кнопка в листе ответа объясняет, чем правильный вариант отличается от выбранного. Карточка знает, ЧТО верно (`meaning_ru`, `explain`), но не знает, почему не подходит именно тот вариант, который ученик выбрал; заготовить это в колоде нельзя — пар сотни.

`src/lib/coach.ts` разделён так же, как `sound.ts`: **сборка** — чистые `buildPrompt` (system и user по `WhyAsk`) и `cacheKey`, их проверяет тест в Node; **сеть** — `askWhy` поверх fetch, поток SSE читается по кускам, отказы приходят человеческой причиной через `CoachError`. Модель — `COACH_MODEL`.

Ключ Anthropic живёт там же, где токен GitHub: поле `anthropicKey` в `Settings`, только в localStorage устройства. В коде и в репозитории его нет; в запрос он уходит единственным заголовком `x-api-key`. Миграции поле не требует — пустая строка и есть дефолт.

Ответ кладётся в хранилище `kv` (`kvGet`/`kvSet` в `db.ts`) под ключом `why:<хэш>`, куда входят вид задания, слово, предложение, правильный ответ и выбор ученика. Та же пара второй раз приходит мгновенно, без сети и без денег; правка предложения в колоде меняет ключ, и старый разбор не всплывёт под новым текстом.

Панель (`.why-panel` в `styles.css`) синяя — в отличие от серого авторского пояснения `.rev-explain`: ученик должен видеть, где говорит колода, а где модель. Кнопка `.why-btn` стоит в нижнем листе, а не в теле ответа: на телефоне тело уходит под лист. При раскрытии и по окончании потока панель подводится к верхней кромке — эффект в `Review.tsx` стоит выше ранних возвратов компонента.

## Тесты

`npm test` = пять наборов подряд: `test/session-sim.test.ts` (симуляция сессий, инварианты очереди и форматов), `test/metrics.test.ts` (цели, темп, бакеты интервалов), `test/math-pilot.test.ts` (математический путь на фикстурах `test/fixtures/math/` — разбор карточек, KaTeX, сверка числового ответа, разведение по разделам), `test/sound.test.ts` (партитуры звука: диапазон частот, огибающая, длительность, сумма одновременных нот, единая тональность), `test/coach.test.ts` (разбор «Почему?»: состав запроса, ключ кэша, склейка потока по произвольным кускам, отказы, ключ только в заголовке — сеть подменяется, настоящих запросов к API тест не делает). Прогон стоит первым шагом в `deploy.yml`: красный тест блокирует выкатку. Проверки помечены кодами (C1, C3, C8…) — код в тесте и в комментарии к коду в `src/` указывают друг на друга.

Фикстуры математики лежат в репозитории намеренно: тест не должен зависеть от наличия vault на машине, иначе на сборке он падает, а локально проходит.

## Сторож дисциплины и валидатор колоды — живут не здесь

Оба переехали в отдельный репозиторий `s4s4s4s/sat-deck` вместе с колодой и журналом: сторож (`check.mjs`, пишет строку отсутствия за уже закрытый учебный день, только за него — день кончается в 04:00) и валидатор карточек. В этом репозитории (`sat-srs`) их искать не нужно — здесь только код приложения.

## Что важно знать

- **Предметы не смешивать.** Слова и математика — только отдельными разделами; interleaving допустим лишь внутри одного предмета. Нарушение этого — регресс, а не фича.
- Правки логики повторений почти всегда идут в `scheduler.ts`; правки «что видно на повторении» — в `screens/Review.tsx`.
- Дефолт настройки доезжает до установленного PWA только через `SETTINGS_VERSION` + миграцию в `store.ts`: сохранённый объект перекрывает `DEFAULT_SETTINGS` навсегда.
- Перевод примера приходит из колоды полем `contexts_ru` и показывается под предложением после ответа. Он выровнен по индексу с `contexts`: контексты ротируются, поэтому `Review.tsx` берёт перевод по позиции показанного предложения, а не первый из списка. Выравнивание стережёт валидатор колоды в `sat-deck`, расхождение там — ошибка, а не предупреждение.
- Источник карточек — репозиторий `s4s4s4s/sat-deck` (ветка `main`), а не эта кодовая база: расхождения между приложением и карточками ищи в `sync.ts` / `github.ts`.
- `scripts/` — вспомогательное: `shots.mjs` (скриншоты), `make-icons.mjs` (иконки PWA), `sound-wav.mjs` (звуки в WAV).
- `package-lock.json` — 256 КБ, читать незачем.
